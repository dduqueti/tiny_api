language: ruby
cache: bundler
rvm:
  - 2.2.4
before_script:
  - cp config/database.travis.yml config/database.yml
  - psql -c 'create database tiny_api_development;' -U postgres
  - bundle exec rake db:migrate --trace
script:
  - RAILS_ENV=test bundle exec rake test
services:
  - redis-server

